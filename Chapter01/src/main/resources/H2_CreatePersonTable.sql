CREATE TABLE Person (
  id int NOT NULL,
  name varchar(20) NOT NULL,
  country varchar(20) DEFAULT NULL,
  PRIMARY KEY (id)
)

ALTER TABLE Person ALTER COLUMN id SET GENERATED BY DEFAULT AS IDENTITY (START WITH 1 INCREMENT BY 1);